pandas>=1.5
pyarrow>=8.0
sqlalchemy>=1.4
flask>=2.0
# Airflow is optional and heavy; uncomment if you plan to run Airflow locally
# apache-airflow>=2.6
python-dotenv>=0.19.0

# Note: this project calls the `kaggle` CLI (subprocess) to download datasets
# when using `ingestion/download_kaggle.py`. The CLI can be installed via
# `pip install kaggle` or your system package manager; it's intentionally not
# required as a runtime dependency in this repository.